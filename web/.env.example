# API base (server side, used by next.config.ts rewrites)
API_BASE=http://localhost:8000

# Public API base (used by lib/api-client.ts in the browser)
NEXT_PUBLIC_API_BASE=/api/v1

# NextAuth (populated in W1)
NEXTAUTH_SECRET=
NEXTAUTH_URL=http://localhost:3000
